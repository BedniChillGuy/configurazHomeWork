=begin
Пример конфигурации для мониторинга веб‑сервиса
=cut
service := q(web-frontend);
port := 0o120;
targets := { q(/health). q(/metrics). };
retry_delay := [+ 0o02 0o04];
full_targets := { service. targets. retry_delay. };

